//ex3 - a function that takes an array of integers, and returns a new array with every other element
//
//START
//
//GET inputArray = input array of integers
//
//SET inputIterator = 0
//SET newArray = array of every other integer from inputArray
//SET newIterator = 0
//SET remainder = remainder value to find alternating iterators
//
//WHILE inputIterator < length of inputArray
//  remainder = inputIterator % 2
//  IF remainder is 0
//    SET inputNumber = inputArray value at space "inputIterator"
//    SET newnumber = newArray value at space "newIterator"
//    newNumber = inputNumber
//    newIterator = newIterator + 1
//
//  inputIterator = inputIterator +1
//
//PRINT newArray
//
//END
